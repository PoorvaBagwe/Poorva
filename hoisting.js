
/*hoisting is a JavaScript mechanism where variables and function declarations are moved to the top of their containing scope during the 
compilation phase. This means that you can use variables and functions before they are declared in the code. This can lead to unexpected behaviour if not understood properly.This can be demonstrated with the following example:
*/


console.log(a);

a=10;